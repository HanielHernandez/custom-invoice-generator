export type Company = {
    name: string
    id?: string
    phoneNumber: string
    zipcode: string
    address: string
    logoUrl: string
    signature: string
    terms: string
    tags: string[]
}
